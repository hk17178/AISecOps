# 复制为 .env 后填真实值（.env 已 gitignore，不入库 —— 见 O4 / CONSTRAINTS C-9）
# 凭证零硬编码：所有密码/Key 走 .env，代码用 pydantic-settings 读取。

# ---- PostgreSQL ----
POSTGRES_USER=aisecops
POSTGRES_PASSWORD=change_me_strong_password
POSTGRES_DB=aisecops
POSTGRES_PORT=5432

# ---- Redis ----
REDIS_PORT=6379

# ---- Grafana（可观测 profile）----
GRAFANA_PASSWORD=admin

# ---- LLM Provider（L05 Gateway 用，S2 起需要）----
# 出域开关默认关；本地优先。按你 PRD §9 选定后填。
# DOUBAO_API_KEY=
# TONGYI_API_KEY=
# ZHIPU_API_KEY=
# OPENAI_COMPAT_BASE_URL=http://localhost:11434/v1   # 本地 Ollama
# ANTHROPIC_API_KEY=                                  # 仅出域开关开启时
