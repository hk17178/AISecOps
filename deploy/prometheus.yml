# Prometheus 最小配置：抓取 OTel Collector 暴露的应用指标
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: aisecops-otel
    static_configs:
      - targets: ["otel-collector:8889"]
  - job_name: prometheus-self
    static_configs:
      - targets: ["localhost:9090"]
